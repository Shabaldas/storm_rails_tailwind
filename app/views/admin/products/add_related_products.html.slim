= semantic_form_for [:admin, resource], builder: ActiveAdmin::FormBuilder do |f|
  = f.inputs do
    = f.input :related_product_ids, input_html: { class: "select2" }, label: 'Tags', collection: Product.where.not(id: resource.id), multiple: true

    = f.actions
