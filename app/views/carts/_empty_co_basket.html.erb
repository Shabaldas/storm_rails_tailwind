<div id="change_picture_in_co">
  <% if current_cart.cart_items.empty? %>
    <div class="w-full h-48 flex flex-col justify-center items-center">
      <%= image_tag("partials_img/empty_basket.png") %>
      <p class="text-base text-gray-400 mt-4">Still empty...</p>
      <%= link_to 'Back to Products', products_path, class: "w-full lg:w-fit h-16 mt-10 bg-yellow-400 flex justify-center items-center rounded-lg text-base px-3 text-center sm:px-0 sm:text-xl p-2 font-medium uppercase" %>
    </div>
  <% end %>
</div>
